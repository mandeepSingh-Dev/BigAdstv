<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BigAdsTV" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/background_theme_color</item>
        <item name="fontFamily">@font/poppins</item>
    </style>

    <style name="rippleStyle" parent="Widget.AppCompat.ImageButton">
        <item name="tabRippleColor">@color/black</item>
        <item name="rippleColor">@color/black</item>
    </style>
    <style name="Theme.BigAdsTV" parent="Base.Theme.BigAdsTV" />

</resources>